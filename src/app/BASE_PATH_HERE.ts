/**
 * Url chave para acesso à rota da API. 
 * 
 * normalmente a estrutura da mesma é a seguinte:
 * BASE_URL = (http|https)://<meu_dominio_aqui>/wp-json
 */
export const BASE_URL = `http://localhost/wordpress/wp-json`;